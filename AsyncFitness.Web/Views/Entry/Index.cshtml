@using AsyncFitness.Core.Models
@model GymCustomer

@{
    ViewData["Title"] = "Welcome";
}

<div class="text-center">
    <button asp-action="Create" type="button" onclick="location.href='@Url.Action("Create", "Entry")'">
        <div >Create Account</div>
    </button>
    <button asp-action="Login" type="button" onclick="location.href='@Url.Action("Login", "Entry")'">
        <div >Login</div>
    </button>
</div>